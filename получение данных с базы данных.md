### 1. Клиент отправляет http-запрос через приложение 
### 2. Запрос отправляется на сервер
### 3. Сервер делает запрос к базе данных
### 4. База данных возвращает результат на сервер
### 5. Сервер отправляет данные обратно клиенту
### 6. Клиент отображает данные в интерфейсе

### Клиент отправляет запрос
На стороне клиента пользователь выполняет действие, которое инициирует запрос данных. Например, пользователь нижаем кнопку или переходит на страницу, где должны быть отображены данные.

(с использованием библиотеки axios)

```javascript
axios.get("http://localhost:3000/books")
  .then(response => {
    console.log(response.data)
  })
  .catch(error => {
    console.log(error)
  })
```

### Запрос отправляется на сервер
Серверное приложение обрабатывает запрос, проверяет его и отправляет запрос к базе данных для получения данных.

```javascript
app.get('/books', (req, res) => {
  db.query('SELECT * FROM books', (err, results) => {
    if (err) {
      return res.status(500).json({ error: 'Database query failed' });
    }
    res.json(results);
  });
});
```

### Сервер взаимодействует с базой данных
Когда сервер получает запрос, он создает SQL-запрос к базе данных для получения нужных данных.
```SQL
  SELECT * FROM books;
```
### База данных возвращает результат серверу
После того, как база данных выполнит запрос, она возвращает результат серверу. Данные возвращаются в виде объектов или строк JSON.

### Сервер отправляет данные клиенту
После получения данных из базы сервер формирует ответ (чаще всего в формате JSON) и отправляет его обратно клиенту через HTTP.
HTTP-ответ с кодом состояния 200 (OK) и телом, содержащим данные.

### Клиент получает и отображает данные
Когда клиент получает данные от сервера, он использует JavaScript для их обработки и отображения на странице. В приложении на React, например, это может быть использование useState и useEffect для хранения и рендеринга данных.
```javascript
  const [books, setBooks] = useState([]);

  useEffect(() => {
    axios.get('http://localhost:3000/api/books')
      .then(response => {
        setBooks(response.data);
      })
      .catch(error => {
        console.error('Error fetching data', error);
      });
  }, []);
```
